name: Publish open-ticket-ai to PyPI

on:
  release:
    types: [published]
  push:
    tags:
      - 'v*'
      - 'open-ticket-ai-v*'
  workflow_dispatch:

jobs:
  publish:
    uses: ./.github/workflows/publish-to-pypi.yml
    with:
      package-name: open-ticket-ai
      package-path: .
    secrets:
      PYPI_API_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
