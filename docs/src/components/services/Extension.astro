---
export interface Props {
  icon: string;
  title: string;
  desc: string;
}

const { icon, title, desc } = Astro.props;

// Map icon names to emoji/unicode symbols as fallback
const iconMap: Record<string, string> = {
  'tune': '⚙️',
  'analytics': '📊',
  'integration_instructions': '🔧',
  'update': '🔄',
};
---

<div class="flex flex-col items-start gap-4">
  <div class="flex h-12 w-12 items-center justify-center rounded-xl bg-primary/10 text-primary text-2xl">
    <span>{iconMap[icon] || '✨'}</span>
  </div>
  <div>
    <h4 class="text-lg font-bold text-white mb-2">{title}</h4>
    <p class="text-slate-400 text-sm leading-relaxed">{desc}</p>
  </div>
</div>
